# 📌 Project Handover Document (项目完整总结与交接文档)

> **文档目的**：本交接文档为 AI Agent 及开发者提供 `bxl-hugo` 博客项目的全面技术架构、设计理念、目录规范、组件实现细节及关键踩坑指南。后续任何全新的 Agent 接入本项目时，请首先阅读本文档。

---

## 一、 项目概述 (Project Overview)

- **项目名称**：`bxl-hugo`
- **技术栈**：Hugo (Extended Version) SSG 静态生成引擎 + Custom Theme (`bxl_style`) + Vanilla CSS + ES6 JS
- **部署方案**：GitHub 源码托管 + Vercel Global Edge Network 自动化构建部署
- **设计版本**：**V5 (Claude Parchment & Dejavu Hybrid Theme)**

---

## 二、 核心美学与双栖设计理念 (Core Design Philosophy)

项目采用了独特的**“主页极客壁纸 + 内页暖米白阅读”**双栖设计架构：

1. **主页 / 落地页 (Landing Page `body.page-landing`)**：
   - 保持原始暗色暗黑壁纸背景 (`/bg.webp` + `rgba(0,0,0,0.4)` 半透明遮罩)；
   - 屏幕两侧保留极客 HUD 监控面板 (`.cyber-left`, `.cyber-right` - FPS/MEM/PING/CLOCK)；
   - 保持 100% 悬浮磨砂玻璃质感。

2. **内页 / 内容页 (Internal Pages `body.page-internal`)**：
   - **背景**：完全移除黑背景壁纸与黑遮罩，重置为温暖护眼的羊皮纸大布背景 (`#FAF8F5`)；
   - **排版字体**：全站接入 Google Fonts 官方的 **思源宋体 (Source Han Serif SC / Noto Serif SC)**，结合高清抗锯齿平滑渲染（`-webkit-font-smoothing: antialiased`），打造如同纸质实体书般的印刷阅读感；
   - **架构**：去繁就简，无框式纯净阅读，移除冗余卡片阴影。

---

## 三、 核心功能组件与实现细节 (Key Components)

### 1. 全局日/夜间模式切换系统 (Light/Dark Mode)
- **触发入口**：`themes/bxl_style/layouts/partials/header.html` 中的 `#themeToggle` 按钮（太阳/月亮 SVG 图标）；
- **响应逻辑**：
  - 按钮绑定直连 `onclick="toggleSiteTheme()"`，并在 `header.html` 中注入 IIFE 脚本，确保 DOM 毫秒级即时响应切换；
  - 自动通过 `localStorage.setItem('theme', 'dark' | 'light')` 持久化存储，并兼容系统 `prefers-color-scheme: dark` 预设；
- **CSS 作用域隔离机制**（*关键架构*）：
  - 浅色模式规则严格作用于 `body.page-internal:not(.dark-mode)`；
  - 深色模式规则作用于 `body.page-internal.dark-mode` (背景 `#18191C`、文字 `#E2E0D8`、卡片 `#24262B`)；
  - 避免了 `!important` 相互锁死导致夜间模式无法切出的问题。

### 2. 手搓 HTML5 音频播放器 (Custom Music Player)
- **彻底抛弃 APlayer**：原第三方 APlayer 压缩 CSS 中存在 `.aplayer-lrc:before/after` 强行白渐变胶囊背景，已被完全移除并替换为纯原生 HTML5 手搓音频控制台；
- **主页音乐控件 (Hero Player `/home/`)**：
  - 移除了最外层大号阴影卡片遮罩（`.home-player` 设为 `background: transparent`），仅保留 `#hero-player` 紧凑核心卡片；
  - 定制了音量拉条 `.hp-volume`：实心轨道（日间 `#C5BCAC` / 夜间 `#4A4D56`）+ 陶土红圆形滑块 (`#C8654B`)；
  - 歌单抽屉 `.playlist-drawer`：日间为 `#EDE9E1`，夜间为 `#24262B`，曲目文字高对比度适配。
- **独立音乐页 (`/music/`)**：
  - 模板位于 `themes/bxl_style/layouts/_default/music.html`；
  - 样式全量重构，包含封面播放、多歌词滚动（`.mp-lrc-inner`）、进度条拖拽、音量调整及底端歌单列表；
  - `music.html` 内部已嵌入完整的日夜双模式 CSS 配色。

### 3. Dejavu 风格文章列表 (Article List Separators)
- **参考来源**：`blog.dejavu.moe/posts/`
- **样式规则**：
  - 文章列表项 `.posts-stream__item` 添加了符合暖米白主题的细实线分隔符 (`border-bottom: 1px solid #E5E0D8`)；
  - 扩充垂直内边距 (`padding: 14px 0`)；
  - 鼠标悬停时链接 `.posts-stream__link` 平滑右移 (`transform: translateX(4px)`) 并高亮为陶土红 (`#C8654B`)。

### 4. Twikoo 评论区适配 (Twikoo Comment System)
- **模板位置**：`themes/bxl_style/layouts/partials/twikoo_comments.html`；
- **样式覆写**：
  - 覆写了原本适配暗色壁纸的 `color: #ffffff` 强发光文字，日间模式下恢复为 `#333333` / `#555555` 高对比度深色；
  - 输入框 `.el-input__inner` / `.el-textarea__inner` 重置为白底（`#FFFFFF`）+ 浅边框（`#D8D1C3`）；
  - 前缀标签（昵称、邮箱、网址）、操作图标与发送按钮均已实现日夜双模式动态切换。

### 5. 工具箱软件下载页 (Tools / Download Page `/tools/`)
- **定位**：`BaiWindowsTools`（系统优化管理工具箱）的单产品官方下载页，仓库地址 `https://github.com/bai0131/BaiWindowsTools`；
- **模板位置**：`themes/bxl_style/layouts/_default/tools.html`，全部样式与脚本内联在模板中（与 `music.html` 同一种做法），**不向 `style.css` 追加任何规则**；
- **区块顺序**（自上而下）：产品头图 → 产品预览（轮播）→ 软件下载 → 未签名说明 → 功能特性 → 架构与可信度 → 系统要求与安装 → 开源与声明。除头图和未签名说明外，每个区块都用 `<h2 class="posts-stream__title">` 作为标题，且内容宽度与标题左右对齐；
- **产品数据集中在 front matter**：`content/tools.md` 的 `tool:` 段（version / releasedate / filename / filesize / platform / downloadurl / repourl / releasesurl / sha256 / mirrors），换版本只需改这一个文件。注意 Hugo 的 `.Params` 键会被转为小写，因此该段所有键名必须小写；
- **主下载按钮**使用 Release 资源直链，点击即开始下载，无中间跳转页；
- **其他下载方式**为数据驱动的网盘镜像列表（`tool.mirrors`，含 `name` / `url` / `note`），当前提供蓝奏云与夸克网盘；列表为空时整个区块不渲染，增删条目无需改模板。注意网盘链接是外部地址，需同步更新版本，页面上的自动更新逻辑只覆盖 GitHub 直链；
- **版本信息自动更新**：模板内嵌原生 JS 请求一次 `api.github.com/.../releases/latest`，刷新版本号／发布日期／文件大小／SHA256，若最新 tag 与 front matter 不一致且存在匹配 `-x64-setup.exe` 的资源，自动把按钮指向新资源直链；请求失败（速率限制、网络不通）或未发布 Release 时，**保留服务端渲染内容**，未发布时按钮降级指向 `/releases`；
- **产品预览为自动轮播**（`tool.gallery` 列表，含 `src` / `alt` / `caption`）：5 秒自动切换，支持左右按钮、指示点、触屏滑动；鼠标悬停、键盘聚焦、标签页切到后台时自动暂停；系统开启「减少动态效果」时只保留手动切换。图片存放在 `static/img/tools/`；宣传图是 2:1、界面截图约 1.64:1，统一用 `object-fit: contain` 放进 16:9 画框以避免裁切内容。列表为空时整个区块不渲染；
- **面板配色**（双模式成对声明，通过 `#tools-page` 上的 CSS 变量下发）：浅色底 `#FAF8F5`、面板 `#EDE9E1`、边框 `#D9D2C5`、主色 `#C8654B`；深色底 `#18191C`、面板 `#24262B`、边框 `#33363D`、主色 `#E5866C`；警示区块浅 `#FBF3E8`/`#8A6A3A`、深 `#2E2A24`/`#D8B98A`。

---

## 四、 关键坑点与开发禁忌 (Critical Pitfalls & Do Not's)

> ⚠️ **新 Agent 必须严格遵守以下规则：**

1. **绝对禁止使用 PowerShell `Set-Content` 或 `Get-Content | Set-Content` 修改 `style.css`**：
   - **原因**：PowerShell 的 UTF8 输出会在文件开头强行写入 **UTF-8 BOM (`EF BB BF`)** 字节头。这会导致 CSS 解析全盘崩溃，中文注释乱码且选择器失效！
   - **正确做法**：必须使用编辑工具的 `replace_file_content` / `write_to_file` 工具，或使用 `[System.IO.File]::WriteAllText` 确保保存为 **UTF-8 NO BOM** 格式。

2. **绝对禁止重新引入 APlayer CSS**：
   - **原因**：APlayer 压缩 CSS 里的渐变选择器优先级极高，会再次引入白胶囊框破坏页面美感。

3. **不要在 `hugo server` 运行时手动删除 `public/css/style.min.*.css`**：
   - **原因**：删除已生成的指纹 CSS 文件会导致 Hugo 内存服务返回 404，浏览器加载裸 HTML 造成“样式丢了”的假象。修改 `style.css` 时只需保存文件，Hugo 会自动热重载。

4. **样式优先级控制**：
   - 编写浅色模式规则时，务必使用 `body.page-internal:not(.dark-mode)` 声明，切勿直接写 `body.page-internal div { color: #111 !important; }`，否则会强行锁死夜间模式的文字颜色。

5. **`style.css` 第 2287 行有一条全局贪婪规则**，对 `body, a, h1~h6, p, span, li, div` 强制了 `color: #f5f5f5`、`font-weight: 600`、`letter-spacing: 0.5px` 三条 `!important`：
   - 由于 `!important` 的声明会压过任何非 `!important` 声明（与特异性无关），新增组件若要自定义这些属性，**必须同样加 `!important`**；
   - 又因为 `:not()` 会贡献其参数的特异性，`body.page-internal:not(.dark-mode) span` 的特异性是 (0,2,2)，普通类选择器难以压过。工具箱页面的做法是给页面容器加 `id="tools-page"`，用 id 选择器（1,2,1）稳定胜出，可作参考。

6. **不要引用 `var(--paper-*)` 与 `var(--text-*)` 系列变量**：
   - 这些 token 只在 `body.dark` 块（`style.css` 81-137 行）中被重新定义过，而 body 上实际挂的类是 `dark-mode`，该块永远不匹配；
   - 因此在深色模式下这些变量仍解析为浅色值，直接引用会渲染出错误的颜色。请改用显式十六进制值。

7. **代码块的文字颜色必须显式指定，不能用 `inherit`**：
   - `style.css:1508` 把 `pre` 的背景写死为深色 `#1e1e1e`（两种模式都是），而 `style.css:1509` 原本写的是 `pre code { color: inherit }`；
   - 浅色模式下 `pre` 会从内部页的 `div` 规则继承到 `#111111`，结果变成黑底黑字、代码完全不可读（深色模式反而正常）；
   - 现已改为 `color: #e6e6e6`。若后续要调整，请保持一个在深色背景上可读的固定值，不要改回 `inherit`。

---

## 五、 项目重要目录结构 (Directory Architecture)

```
bxl-hugo/
├── content/                     # Markdown 文章与单页内容
│   ├── home.md                  # 首页主内容 (layout: myhome)
│   ├── about.md                 # 关于页面内容 (包含 V1~V5 演进日志)
│   ├── music.md                 # 音乐页面内容 (layout: music)
│   ├── comments.md              # 留言板页面内容 (layout: comments)
│   ├── tools.md                 # 工具箱下载页内容 (layout: tools，产品数据在此)
│   └── posts/                   # 博客文章目录
├── themes/
│   └── bxl_style/              # 自定义 Hugo 主题目录
│       ├── layouts/
│       │   ├── _default/
│       │   │   ├── baseof.html  # 基础骨架模板
│       │   │   ├── myhome.html  # 首页布局模板
│       │   │   ├── music.html   # 音乐页专属布局模板 (样式与脚本内联)
│       │   │   ├── tools.html   # 工具箱下载页模板 (样式与脚本内联)
│       │   │   ├── comments.html # 留言板页面模板
│       │   │   ├── list.html    # 文章列表页模板
│       │   │   └── single.html  # 文章详情页模板
│       │   └── partials/
│       │       ├── head.html    # <head> 头模板 (引入思源宋体与 CSS 资源)
│       │       ├── header.html  # 顶部导航栏与日夜模式切换按钮/脚本
│       │       ├── footer.html  # 页脚模板
│       │       ├── scripts.html # 全局 JS 资源加载
│       │       ├── posts_list_stream.html # 首页文章流组件
│       │       └── twikoo_comments.html  # Twikoo 评论区初始化组件
│       └── assets/
│           ├── css/
│           │   └── style.css    # 全站核心样式文件 (~3700+ 行)
│           └── js/
│               └── main.js      # 主脚本 (主题持久化逻辑)
├── hugo.toml                    # Hugo 站点全局配置文件
└── PROJECT_HANDOVER.md          # 本交接文档
```

---

## 六、 常用开发指令 (Developer Commands)

```bash
# 本地启动 Hugo 实时预览 (禁用快速渲染 + 清理目标)
hugo server --disableFastRender --cleanDestinationDir

# 构建生产环境代码
hugo --minify

# Git 提交与推送
git add .
git commit -m "your commit message"
git push
```

---
*文档生成时间：2026-09-15*
*维护者：Antigravity Agent Team*
